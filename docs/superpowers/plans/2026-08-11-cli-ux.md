# CLI 交互界面重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重构 `npm start` 交互模式为 ASCII 艺术横幅 + 三步引导流程（检测→确认→执行+完成引导），改善视觉与用户引导。

**Architecture:** 新建纯展示模块 `src/ui.js`（函数返回字符串，调用方打印，便于测试）；`src/platform.js` 增加 `isCursorRunning()` 与 `readCursorVersion()`；`index.js` 的 `runInteractive()` 重构为三步引导。静默模式 `--action` 不动。

**Tech Stack:** Node.js 18, CommonJS, chalk ^4, inquirer ^8。

## Global Constraints

- 项目无测试框架，验证用 `scripts/verify-*.js` 脚本模式
- 静默模式 `--action=translate|restore` 行为必须完全不变（脚本/CI 兼容）
- 路径探测、提权、i18n-core 翻译逻辑一律不动
- 代码风格：2 空格缩进、单引号、无尾逗号、中文注释
- 终端无颜色环境（非 TTY）不得崩溃

---

### Task 1: `src/ui.js` 展示组件库

**Files:**
- Create: `src/ui.js`
- Test: `scripts/verify-ui.js`

**Interfaces:**
- Consumes: `chalk`（已依赖）
- Produces:
  - `banner(version)` → string：ASCII 标题 + 副标题
  - `step(index, total, label)` → string：`── 步骤 1/3  文案 ──`
  - `ok(msg)/warn(msg)/err(msg)/info(msg)` → string：徽章消息（无缩进，调用方加）
  - `section(title)` → string：分节标题 + 下划线
  - `divider()` → string：分隔线
  - `fileList(items)` → string：`items: [{left, right}]`，left 列按全角宽对齐
  - `displayWidth(s)` → number：全角字符按 2 列宽计算

- [ ] **Step 1: 写失败测试 `scripts/verify-ui.js`**

```js
// 验证 src/ui.js 展示组件（捕获返回值断言内容）
const assert = require('assert');
const ui = require('../src/ui.js');

const b = ui.banner('2.0.0');
assert.ok(b.includes('Cursor-zh'), 'banner 应含标题');
assert.ok(b.includes('v2.0.0'), 'banner 应含版本号');

assert.ok(ui.step(1, 3, '检测').includes('步骤 1/3'), 'step 应含步骤编号');
assert.ok(ui.ok('x').includes('✔'), 'ok 应含 ✔');
assert.ok(ui.warn('x').includes('⚠'), 'warn 应含 ⚠');
assert.ok(ui.err('x').includes('✖'), 'err 应含 ✖');
assert.ok(ui.info('x').includes('ℹ'), 'info 应含 ℹ');
assert.ok(ui.section('常见问题').includes('常见问题'), 'section 应含标题');
assert.ok(ui.divider().includes('─'), 'divider 应含横线');

assert.strictEqual(ui.displayWidth('abc'), 3, 'ASCII 按 1');
assert.strictEqual(ui.displayWidth('中文'), 4, '中文按 2');

const fl = ui.fileList([
  { left: ui.info('安装路径'), right: 'D:/x' },
  { left: ui.info('版本号'), right: '3.15.6' },
]);
assert.ok(fl.split('\n').length === 2, 'fileList 两行');
assert.ok(fl.includes('D:/x'), 'fileList 含 right');

console.log('✅ verify-ui 全部通过');
```

- [ ] **Step 2: 运行确认失败**

Run: `node scripts/verify-ui.js`
Expected: `MODULE_NOT_FOUND: Cannot find module '../src/ui.js'`

- [ ] **Step 3: 实现 `src/ui.js`**

```js
/**
 * 纯展示 UI 组件库：所有函数返回字符串，调用方 console.log。
 * 无业务逻辑、无副作用，便于测试与复用。
 */
const chalk = require('chalk');

const LOGO = [
    ' ###  #   # ####   ###   ###  ####        ##### #   # ',
    '#     #   # #   # #     #   # #   #          #  #   # ',
    '#     #   # #   #  ###  #   # #   # #####   #   ##### ',
    '#     #   # #  #      # #   # #  #         #    #   # ',
    ' ###   ###  #   #  ###   ###  #   #       ##### #   # ',
];

function banner(version) {
    return `\n${LOGO.map(l => chalk.cyan(l)).join('\n')}\n\n`
        + `${chalk.white.bold('  Cursor-zh')} ${chalk.gray(`v${version || '?'}`)}\n`
        + `${chalk.gray('  Cursor 本地汉化工具 · 一键汉化 / 随时还原')}\n`;
}

function step(index, total, label) {
    return `\n  ${chalk.cyan.bold(`── 步骤 ${index}/${total}  ${label} ──`)}\n`;
}

const ok = (msg) => `${chalk.green('✔')} ${msg}`;
const warn = (msg) => `${chalk.yellow('⚠')} ${msg}`;
const err = (msg) => `${chalk.red('✖')} ${msg}`;
const info = (msg) => `${chalk.cyan('ℹ')} ${msg}`;

function section(title) {
    return `\n  ${chalk.blue.bold(title)}\n  ${chalk.blue('─'.repeat(Math.max(20, /* 全角对齐 */ title.length * 2)))}\n`;
}

function divider() {
    return chalk.gray('  ' + '─'.repeat(44));
}

/** 全角字符（中文/emoji）按 2 列宽计算 */
function displayWidth(s) {
    let w = 0;
    for (const ch of s) {
        w += /[\u1100-\u115f\u2e80-\ua4cf\uac00-\ud7a3\uf900-\ufaff\ufe30-\ufe4f\uff00-\uff60\uffe0-\uffe6一-鿿]/.test(ch) ? 2 : 1;
    }
    return w;
}

/** items: [{ left, right }] — left 列按全角宽对齐到 28 列 */
function fileList(items) {
    const lines = items.map(it => {
        const pad = Math.max(2, 28 - displayWidth(it.left));
        return `  ${it.left}${' '.repeat(pad)}${it.right || ''}`;
    });
    return lines.join('\n').replace(/[ \t]+$/gm, '');
}

module.exports = { banner, step, ok, warn, err, info, section, divider, fileList, displayWidth };
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node scripts/verify-ui.js`
Expected: `✅ verify-ui 全部通过`

- [ ] **Step 5: 提交**

```bash
git add src/ui.js scripts/verify-ui.js
git commit -m "feat: 新增 src/ui.js 展示组件库 + 验证脚本"
```

---

### Task 2: `src/platform.js` 增加进程检测与版本读取

**Files:**
- Modify: `src/platform.js`（追加两个函数 + 导出）
- Test: `scripts/verify-platform.js`

**Interfaces:**
- Consumes: 既有 `fs`、`path`、`PLATFORM`
- Produces:
  - `isCursorRunning()` → boolean
  - `readCursorVersion(appPath)` → string|null

- [ ] **Step 1: 写失败测试 `scripts/verify-platform.js`**

```js
const assert = require('assert');
const path = require('path');
const { isCursorRunning, readCursorVersion } = require('../src/platform.js');

const APP = process.argv[2] || 'D:/Program Files/cursor/resources/app';

// readCursorVersion：读真实安装的 product.json，应返回非空版本
const v = readCursorVersion(APP);
assert.ok(typeof v === 'string' && v.length > 0, `应读到版本号，实际: ${v}`);
console.log('✅ 版本号:', v);

// isCursorRunning：返回布尔即可（不崩溃）
assert.strictEqual(typeof isCursorRunning(), 'boolean');
console.log('✅ 运行检测:', isCursorRunning() ? 'Cursor 运行中' : 'Cursor 未运行');
console.log('✅ verify-platform 全部通过');
```

- [ ] **Step 2: 运行确认失败**

Run: `node scripts/verify-platform.js`
Expected: `TypeError: isCursorRunning is not a function`

- [ ] **Step 3: 实现（追加到 `src/platform.js` 末尾，`module.exports` 前）**

```js
/**
 * 检测 Cursor 进程是否在运行（汉化前应完全退出，否则 state.vscdb 步骤会跳过）
 */
function isCursorRunning() {
    const { execSync } = require('child_process');
    if (PLATFORM === 'win32') {
        try {
            const out = execSync('tasklist /FI "IMAGENAME eq Cursor.exe" /NH', {
                encoding: 'utf8',
                stdio: ['pipe', 'pipe', 'ignore'],
                timeout: 5000,
            });
            return /Cursor\.exe/i.test(out);
        } catch {
            return false;
        }
    }
    try {
        execSync('pgrep -x Cursor || pgrep -x cursor', { stdio: 'ignore', timeout: 5000 });
        return true;
    } catch {
        return false;
    }
}

/**
 * 读取 Cursor 版本号（product.json 的 version 字段）
 * @param {string} appPath Cursor resources/app 目录
 * @returns {string | null}
 */
function readCursorVersion(appPath) {
    try {
        const product = JSON.parse(fs.readFileSync(path.join(appPath, 'product.json'), 'utf8'));
        return typeof product.version === 'string' ? product.version : null;
    } catch {
        return null;
    }
}
```

在 `module.exports` 中追加：

```js
    isCursorRunning,
    readCursorVersion,
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node scripts/verify-platform.js`
Expected: `✅ verify-platform 全部通过`（版本号 = 真实安装版本）

- [ ] **Step 5: 提交**

```bash
git add src/platform.js scripts/verify-platform.js
git commit -m "feat: platform 增加 isCursorRunning/readCursorVersion"
```

---

### Task 3: `index.js` 重构 runInteractive — 步骤 1 检测展示

**Files:**
- Modify: `index.js`（顶部 require + 新增状态检测 helper + 替换 runInteractive 前半）

**Interfaces:**
- Consumes: `ui.banner/step/ok/warn/err/info/fileList/divider`、`platform.isCursorRunning/readCursorVersion`、既有 `obtainCursorPaths`
- Produces: `runInteractive()` 的新三步流程（Task 4 补步骤 3）

- [ ] **Step 1: 顶部 require 更新**

在 `index.js` 顶部（`./src/platform` 的 require 中）追加 `isCursorRunning, readCursorVersion`，并新增：

```js
const ui = require('./src/ui');
const fs = require('fs');
const path = require('path');
const { version } = require('./package.json');
```

- [ ] **Step 2: 新增汉化状态检测 helper（放在 `parseAction` 前）**

```js
/** 检测主 JS 是否已汉化（含字面中文；minified 英文包非 ASCII 全为 \\u 转义） */
function isBundleTranslated(mainJsPath) {
    try {
        const content = fs.readFileSync(mainJsPath, 'utf8');
        return (content.match(/[一-鿿]/g) || []).length > 50;
    } catch {
        return false;
    }
}
```

- [ ] **Step 3: 替换 `runInteractive` 的启动 + 步骤 1（保留原有 obtainCursorPaths 调用）**

将 `runInteractive` 开头改为：

```js
async function runInteractive() {
    console.log(ui.banner(version));

    // ── 步骤 1: 检测 Cursor 状态 ──
    console.log(ui.step(1, 3, '检测 Cursor'));
    const paths = await obtainCursorPaths();
    if (!paths) {
        await waitForExit();
        return;
    }

    const writable = hasWritePermission(paths.mainJsPath);
    const running = isCursorRunning();
    const translated = isBundleTranslated(paths.mainJsPath);
    const ver = readCursorVersion(paths.appPath);

    console.log(ui.fileList([
        { left: ui.ok('安装路径'), right: paths.appPath },
        { left: ui.info('版本号'), right: ver ? `Cursor ${ver}` : chalk.gray('未知') },
        { left: writable ? ui.ok('写入权限') : ui.warn('写入权限'), right: writable ? '可直接修改' : '将请求管理员权限' },
        { left: running ? ui.warn('运行状态') : ui.info('运行状态'), right: running ? 'Cursor 正在运行，汉化前请先完全退出' : '未运行' },
        { left: translated ? ui.warn('汉化状态') : ui.info('汉化状态'), right: translated ? '当前已汉化，可恢复英文后重新汉化' : '当前为英文原版' },
    ]));
    console.log(ui.divider());
```

（`paths.appPath` 打印、后续步骤保留给 Task 4）

- [ ] **Step 4: 语法与冒烟验证**

Run:
```bash
node --check index.js
node index.js --action=translate --cursor-path="Z:/nonexistent" 2>&1 | head -3
```
Expected: 语法 OK；静默模式报错退出（`❌ 找不到 Cursor 安装目录` + exit 1），证明既有静默路径未破坏。

- [ ] **Step 5: 提交**

```bash
git add index.js
git commit -m "feat: index 交互模式步骤1 检测展示(版本/权限/运行/汉化状态)"
```

---

### Task 4: `index.js` 步骤 2 确认 + 步骤 3 执行引导 + FAQ

**Files:**
- Modify: `index.js`（runInteractive 剩余部分 + showFaq + 完成引导）

**Interfaces:**
- Consumes: Task 3 的 `ui`、`isBundleTranslated`、`paths`
- Produces: 完整可用的 `runInteractive`

- [ ] **Step 1: 新增 FAQ 数据与 `showFaq`（放 `runInteractive` 前）**

```js
const FAQ = [
    ['汉化后界面没变化？', '请完全退出并重启 Cursor；确认定位的是正在使用的安装目录；Cursor 更新后需重新运行汉化。'],
    ['提示"安装已损坏"？', '工具已自动修复校验值。若仍出现，先"恢复英文"再重新汉化，或检查是否被安全软件阻止写入。'],
    ['模型参数（如 Thinking intensity）过一会儿恢复英文？', '工具已注入显示层映射，一般不会再恢复。若出现，重新运行汉化即可。'],
    ['如何完全退出 Cursor？', '右键托盘图标选择"退出"，并在任务管理器中确认 Cursor.exe 已结束。'],
];

async function showFaq() {
    console.log(ui.section('常见问题'));
    for (const [q, a] of FAQ) {
        console.log(`  ${chalk.white.bold(q)}`);
        console.log(`    ${chalk.gray(a)}`);
        console.log('');
    }
}
```

- [ ] **Step 2: 替换 runInteractive 的步骤 2 + 3**

将 `runInteractive` 中 `console.log(ui.divider());` 之后的内容替换为：

```js
    // ── 步骤 2: 确认操作 ──
    console.log(ui.step(2, 3, '确认操作'));
    const targets = [
        ['主窗口', paths.mainJsPath],
        ['附加窗口（Agent/Glass）', paths.glassJsPath],
        ['原生提示文案', paths.nlsMessagesPath],
        ['托盘菜单', paths.mainProcessJsPath],
        ['校验值文件', paths.productJsonPath],
    ].filter(([, p]) => p && fs.existsSync(p));
    console.log(ui.info('将处理以下文件：'));
    console.log(ui.fileList(targets.map(([name, p]) => ({ left: ui.info(name), right: path.basename(p) }))));
    console.log(ui.info('其他：clp 语言包缓存 / 用户扩展 / state.vscdb（按需自动处理）'));
    console.log('');

    // ── 操作选择 ──
    let action;
    while (true) {
        const res = await inquirer.prompt([
            {
                type: 'list',
                name: 'action',
                message: chalk.white.bold('请选择操作：'),
                choices: [
                    { name: chalk.green('🚀 一键汉化'), value: 'translate' },
                    { name: chalk.yellow('⏪ 恢复英文'), value: 'restore' },
                    { name: chalk.cyan('📖 查看常见问题'), value: 'faq' },
                    new inquirer.Separator(),
                    { name: chalk.gray('❌ 退出'), value: 'exit' },
                ],
            },
        ]);
        action = res.action;
        if (action === 'faq') {
            await showFaq();
            continue; // 返回菜单
        }
        break;
    }

    if (action === 'exit') {
        console.log(chalk.gray('\n  再见！👋'));
        return;
    }

    // ── 步骤 3: 执行 ──
    console.log(ui.step(3, 3, action === 'translate' ? '执行汉化' : '恢复英文'));
    const needElevation = !hasWritePermission(paths.mainJsPath);
    if (needElevation) {
        console.log(ui.warn('修改 Cursor 核心文件需要管理员权限。'));
        console.log(ui.info('请在系统弹窗中确认授权，授权后会自动继续。'));
        console.log('');
        try {
            await elevateAndRun(action, paths.appPath);
            console.log('');
            console.log(`${ui.ok(chalk.green.bold('操作已在管理员权限下完成！'))}\n`);
        } catch (e) {
            console.log('');
            console.log(`${ui.err(chalk.red.bold('提权失败或用户取消'))} ${chalk.red(e.message)}\n`);
            await waitForExit();
            return;
        }
    } else {
        if (action === 'translate') {
            translate(paths);
        } else {
            restore(paths);
        }
    }

    // ── 完成引导 ──
    console.log(ui.section(action === 'translate' ? '汉化完成，接下来…' : '还原完成，接下来…'));
    if (action === 'translate') {
        console.log(ui.warn('请完全退出并重启 Cursor，界面文案才会更新。'));
        console.log(`  ${chalk.gray('验证：')} 设置页、菜单、Agent 窗口应出现中文。`);
        console.log(`  ${chalk.gray('备份：')} 原版文件保存在 .backup 文件旁。`);
        console.log(`  ${chalk.gray('还原：')} 随时可再次运行本工具选"恢复英文"。`);
    } else {
        console.log(ui.ok('已还原英文原版，重启 Cursor 生效。'));
    }
    console.log('');
    await waitForExit();
```

- [ ] **Step 3: 语法 + 静默回归验证**

Run:
```bash
node --check index.js
node index.js --action=restore --cursor-path="Z:/nonexistent" 2>&1 | head -3
```
Expected: 语法 OK；restore 静默路径报错退出（不翻译、不修改任何文件）。

- [ ] **Step 4: 提交**

```bash
git add index.js
git commit -m "feat: index 交互模式步骤2确认+步骤3执行+完成引导+FAQ"
```

---

### Task 5: 集成验证

**Files:**
- Test: 手动走查（无代码改动）

- [ ] **Step 1: 手动走查清单**

Run `npm start`，逐项确认：
1. ASCII 标题横幅 + 版本号正常显示
2. 步骤 1 状态行：安装路径/版本号/写入权限/运行状态/汉化状态 5 行正确（当前应显示"已汉化"或按实际）
3. 步骤 2 文件清单列出存在的文件；操作选择含 4 项
4. 选「查看常见问题」→ FAQ 显示 → 返回菜单
5. 选「退出」→ 正常退出
6. 若当前无权限：提权解释文案出现，取消不崩溃
7. 若 Cursor 运行中：运行状态警告出现

- [ ] **Step 2: 静默模式回归**

Run: `node index.js --action=translate --cursor-path="D:/Program Files/cursor/resources/app"`
Expected: 不进入交互界面，直接执行并退出（行为与重构前一致）。

- [ ] **Step 3: 提交（如有修复）**

```bash
git add -A
git commit -m "fix: 交互模式走查修复"
```

---

## Self-Review

**Spec 覆盖：**
- 视觉美化（banner/徽章/分节/步骤）→ Task 1 + Task 3/4 ✓
- 流程引导（运行检测/改前清单/完成引导/提权解释）→ Task 3（运行检测+提权状态行）+ Task 4（清单/提权解释/完成引导/FAQ）✓
- 信息透明（版本/路径来源/权限/汉化状态）→ Task 3 状态行 ✓
- 平衡用户 → 默认简洁 + 关键强引导 ✓
- 路径处理（自动搜索+手动输入+来源展示）→ 沿用 obtainCursorPaths；来源展示在 Task 3 fileList ✓
- 静默模式不动 → Task 3/4 明确保留 ✓

**占位符扫描：** 无 TBD/TODO；所有代码步骤含完整实现 ✓

**类型一致性：** `ui.banner/step/ok/.../fileList`、`platform.isCursorRunning/readCursorVersion`、`isBundleTranslated` 在 Task 1-4 签名一致 ✓